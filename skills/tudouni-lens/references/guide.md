# 接待备忘

有具体创作需求就直接承接。未连接时由助手运行 setup 打开配置页，客户只输入 Key；保存后继续原需求。doctor 是助手诊断工具，不把原始输出、路径或命令交给客户。型号报 ID 原文，没上的品类不要提。

1K 草稿、2K 常用、4K 更细。默认 1K。

客户说「image-2.5 2k」→ `--model gpt-image-2.5 --tier 2k`。

首次连接或更换 Key → 由助手运行 setup 或 setup --replace，客户只在打开的配置窗口粘贴 Key 并保存。Codex 与 Claude Code 使用同一流程；程序创建目录并保留已选中的凭据来源，不让客户创建文件或在目录间搬移 Key。不要根据客户端名称推断文件访问限制。Key 不进入聊天或命令参数。

文字模型拒绝。

价：只在第一次打开说去广场看一眼。出图出视频过程中禁止再提价。
