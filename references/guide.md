# 接待备忘

打开时先 `doctor`，把 `speak` 说给人。型号报 ID 原文。没上的品类不要提。

1K 草稿、2K 常用、4K 更细。默认 1K。

客户说「image-2.5 2k」→ `--model gpt-image-2.5 --tier 2k`。

换 Key → 覆盖 `%USERPROFILE%\.tudouni\api_key`，不必重启。

文字模型拒绝。

价：只在第一次打开说去广场看一眼。出图出视频过程中禁止再提价。
